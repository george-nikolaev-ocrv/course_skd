
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ВыполнитьПрограммнуюОбработку();
КонецПроцедуры
&НаСервере
Процедура ВыполнитьПрограммнуюОбработку()
	Данные = Новый ТаблицаЗначений();
	Отчеты.ОтчетПоНоменклатуре.ПолучитьДанные(Данные, "ДляПрограммногоИспользования");
	
	Для Каждого Элемент Из Данные Цикл
		Сообщить(Элемент.КодТовара + " " + Элемент.Цена);
	КонецЦикла;
КонецПроцедуры