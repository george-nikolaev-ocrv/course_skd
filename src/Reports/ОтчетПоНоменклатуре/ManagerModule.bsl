Процедура ПолучитьДанные(ТаблицаДанных, ИмяВариантаНастроек) Экспорт
	Отчет = Создать();
	Настройки = Отчет.СхемаКомпоновкиДанных.ВариантыНастроек.Найти(ИмяВариантаНастроек);
	Если Настройки = Неопределено Тогда
		ВызватьИсключение "Вариант """ + ИмяВариантаНастроек + """ не найден"; 
	КонецЕсли;
	Отчет.КомпоновщикНастроек.ЗагрузитьНастройки(Настройки.Настройки);
	Отчет.СкомпоноватьРезультат(ТаблицаДанных);
КонецПроцедуры